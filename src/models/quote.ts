class Quote {
    language: string;
    category: string;
    sentence: string;

    constructor(language: string, category: string, sentence: string) {
        this.language = language;
        this.category = category;
        this.sentence = sentence;
    }  
}

export default Quote;
